{
  "private": true,
  "homepage": "https://arcade.medz.dev",
  "scripts": {
    "build": "flutter build web --wasm --no-web-resources-cdn",
    "check": "flutter analyze && flutter test && bun run build && wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "predeploy": "bun run build"
  },
  "devDependencies": {
    "wrangler": "^4.110.0"
  }
}
